include "socklib.h"
int main(){
    struct server_sock server = server_socket(2,1,false);
    server.start(&server,5,1337);
    struct client_sock client = server.accept(server);
    const char* msg = client.recv_msg(&client.handle);
    client.send_msg(&client.handle,msg,strlen(msg));
}